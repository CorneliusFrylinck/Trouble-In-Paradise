import { observer } from "mobx-react-lite";
import React from "react";

const BuildingComponent = () => {
    return (
        <div>BuildingComponent</div>
    )
}

export default observer(BuildingComponent);